import 'package:flutter/material.dart';

class BaoXiu extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Bao Xiu"),
      ),
      body: Center(
        child: Text("This is BaoXiu page"),
      ),
    );
  }
}
