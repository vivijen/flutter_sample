import 'package:flutter/material.dart';

class BaoShi extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Bao Shi"),
      ),
      body: Center(
        child: Text("This is BaoShi page"),
      ),
    );
  }
}
