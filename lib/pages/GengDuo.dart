import 'package:flutter/material.dart';

class GengDuo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Geng Duo"),
      ),
      body: Center(
        child: Text("This is GengDuo page"),
      ),
    );
  }
}
